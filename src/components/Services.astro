---
import { services, person } from "../data/profile";
import Icon from "./Icon.astro";

/** 問い合わせは mailto に寄せる。予約基盤を挟まない分、壊れる箇所が減る */
const mailto = (title: string) =>
  `mailto:${person.email}?subject=${encodeURIComponent(`[${title}] のご相談`)}`;
---

<section class="section section--sunken" id="services">
  <div class="wrap">
    <header class="reveal">
      <p class="eyebrow">Work with me / 08</p>
      <h2 class="section-title">
        <span class="ja">「こんなの無理かも」から。</span>
        <span class="en">Start with the impossible idea.</span>
      </h2>
      <p class="section-lede">
        <span class="ja">
          まだ粗いアイデアからで大丈夫。<br />
          最短で動く形を一緒に見つけます。
        </span>
        <span class="en">
          A rough idea is enough.<br />
          We’ll find the shortest path to something real.
        </span>
      </p>
    </header>

    <ul class="grid stagger">
      {
        services.map((s) => (
          <li class:list={["plan", "reveal", s.featured && "plan--featured"]}>
            {s.featured && <span class="blob" aria-hidden="true" />}
            <p class:list={["badge", `badge--${s.tone}`]}>
              <span class="ja">{s.badge.ja}</span>
              <span class="en">{s.badge.en}</span>
            </p>

            <h3>
              <span class="ja">{s.title.ja}</span>
              <span class="en">{s.title.en}</span>
            </h3>
            <p class="sub">
              <span class="ja">{s.subtitle.ja}</span>
              <span class="en">{s.subtitle.en}</span>
            </p>

            <p class="price">
              <span class="ja">
                {s.originalPrice && (
                  <s>
                    <span class="sr-only">通常価格 </span>
                    {s.originalPrice.ja}
                  </s>
                )}
                {s.price.ja}
                <small>{s.unit.ja}</small>
              </span>
              <span class="en">
                {s.originalPrice && (
                  <s>
                    <span class="sr-only">Regular price </span>
                    {s.originalPrice.en}
                  </s>
                )}
                {s.price.en}
                <small>{s.unit.en}</small>
              </span>
            </p>

            <ul class="points">
              {s.points.map((p) => (
                <li>
                  <Icon name="check" size={17} />
                  <span class="ja">{p.ja}</span>
                  <span class="en">{p.en}</span>
                </li>
              ))}
            </ul>

            <a class="cta" href={mailto(s.title.ja)}>
              <span class="ja">{s.cta.ja}</span>
              <span class="en">{s.cta.en}</span>
            </a>
          </li>
        ))
      }
    </ul>

  </div>
</section>

<style>
  .grid {
    margin-top: 52px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    align-items: stretch;
  }

  .plan {
    display: flex;
    flex-direction: column;
    padding: 30px 26px;
    background: var(--surface-raised);
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    transition:
      transform var(--dur) var(--ease-out),
      box-shadow var(--dur) var(--ease-out);
  }

  .plan:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-lg);
  }

  /* 推したいものだけ反転させる。色数を増やさずに序列をつける */
  .plan--featured {
    position: relative;
    background:
      radial-gradient(110% 80% at 85% 0%, rgba(255, 92, 35, .32), transparent 48%),
      radial-gradient(80% 70% at 0% 100%, rgba(70, 37, 255, .25), transparent 58%),
      var(--surface-dark);
    color: var(--text-on-dark);
    border-color: transparent;
    overflow: hidden;
  }

  /* 原典の cblob。反転面の角に光を置いて、平らな黒に見えないようにする */
  .blob {
    position: absolute;
    top: -60px;
    right: -40px;
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: radial-gradient(circle, var(--accent), transparent 70%);
    opacity: 0.4;
    filter: blur(10px);
    pointer-events: none;
  }

  .badge {
    align-self: flex-start;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    padding: 5px 11px;
    border-radius: var(--radius-pill);
    background: var(--surface-sunken);
    color: var(--text-muted);
  }

  /* カードごとに色を変えて、4つが同じものの並びに見えないようにする */
  .badge--1 {
    background: var(--pastel-1);
    color: var(--on-pastel-1);
  }
  .badge--2 {
    background: var(--pastel-2);
    color: var(--on-pastel-2);
  }
  .badge--3 {
    background: var(--pastel-3);
    color: var(--on-pastel-3);
  }
  .badge--4 {
    background: var(--pastel-4);
    color: var(--on-pastel-4);
  }

  h3 {
    margin-top: 18px;
    font-size: 21px;
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: var(--tracking-tight);
  }

  /* 4枚が横に並ぶ幅では、英語の "AI & technical consultation" だけが2行になり、
     そのカードだけ価格の行が下にずれる。見出しの高さを2行ぶんで揃えて、
     価格の高さを4枚で合わせる。日本語はすべて1行なので影響しない */
  @media (min-width: 1041px) {
    :global([data-lang="en"]) h3 {
      min-height: 2lh;
    }
  }

  .sub {
    margin-top: 4px;
    font-size: 13.5px;
    color: var(--text-muted);
  }

  .plan--featured .sub {
    color: var(--text-on-dark-muted);
  }

  .price {
    margin-top: 20px;
    font-size: 34px;
    font-weight: 600;
    letter-spacing: var(--tracking-display);
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }

  .price small {
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0;
    color: var(--text-faint);
  }

  /* 値下げ前の額。現在価格より小さく淡くして、二重価格が主役にならないようにする */
  .price s {
    margin-right: 9px;
    font-size: 19px;
    font-weight: 500;
    color: var(--text-faint);
    text-decoration-thickness: 1px;
    text-underline-offset: 0;
  }

  .plan--featured .price s {
    color: var(--text-on-dark-muted);
  }

  .plan--featured .price small {
    color: var(--text-on-dark-muted);
  }

  .points {
    display: flex;
    flex-direction: column;
    gap: 11px;
    margin: 20px 0 26px;
  }

  .points li {
    display: flex;
    align-items: flex-start;
    gap: 9px;
    font-size: 13.5px;
    line-height: 1.45;
    color: var(--text-muted);
  }

  .plan--featured .points li {
    color: var(--text-on-dark-muted);
  }

  .points :global(svg) {
    flex: none;
    margin-top: 1px;
    color: var(--accent);
    opacity: 0.85;
  }

  .plan--featured .points :global(svg) {
    color: var(--accent-on-dark);
  }

  .cta {
    margin-top: auto;
    text-align: center;
    font-size: 14px;
    font-weight: 600;
    padding: 12px 16px;
    border-radius: var(--radius-pill);
    background: var(--ink);
    color: var(--paper);
    transition:
      transform var(--dur-fast) var(--ease-out),
      box-shadow var(--dur);
  }

  .plan--featured .cta {
    background: var(--paper-inv);
    color: var(--ink);
  }

  .cta:hover {
    transform: translateY(-1px);
    box-shadow: var(--shadow);
  }

  @media (max-width: 1040px) {
    .grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 620px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
