{
  "name": "maepace-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:sites": "pnpm build && node scripts/prepare-sites.mjs",
    "preview": "astro build && wrangler dev",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.1.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/three": "^0.185.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.112.0"
  }
}
